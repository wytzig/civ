# Behavior

- For simple shell commands (ls, pwd, cat, etc.), respond with raw output only. No preamble, no summary.
- Keep responses concise by default. Avoid restating what the output already shows.
